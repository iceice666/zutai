[package]
name = "zutai-hir"
description = "High-level IR and lowering (CST → HIR) for Zutai general mode (.zt)."
edition.workspace = true
version.workspace = true

[dependencies]
zutai-syntax = { path = "../syntax" }
text-size = "1.1"
rustc-hash = "2.0.0"

[dev-dependencies]
expect-test = "1.5"
