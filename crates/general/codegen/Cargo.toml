[package]
name = "zutai-codegen"
description = "LLVM IR code generation from SSA for Zutai."
edition.workspace = true
version.workspace = true

[dependencies]
zutai-ssa = { path = "../ssa" }

[dev-dependencies]
zutai-syntax = { path = "../syntax" }